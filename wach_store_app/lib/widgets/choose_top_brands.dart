import 'package:flutter/material.dart';

class ChooseTopBrands extends StatelessWidget {
  const ChooseTopBrands({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text(
      'Choose Your Top Brands',
      style: TextStyle(
          fontSize: 24, fontFamily: 'Crimson', fontWeight: FontWeight.w900),
    );
  }
}
